version: ~> 1.0
import:
- ELiTLtd/travis-build-configs:aws.yml@master
language: common-lisp
sudo: required
services:
- docker
env:
  global:
  - ECR_ENDPOINT=689917379567.dkr.ecr.eu-west-1.amazonaws.com
  - DOCKER_REPO=pgloader
script:
- "./build-docker.sh"
notifications:
  email: false
